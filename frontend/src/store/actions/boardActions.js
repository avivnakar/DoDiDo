import { boardService } from '../../services/boardService';

// export async function addCard() {
//     const user = await boardService
//     return dispatch => dispatch({ type: 'LOGIN', user });
// }
